@model IEnumerable<Suomenkieli.VocabularyList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@if (Model != null && Model.Count() > 0)
{
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
            @*<th>
                    @Html.DisplayNameFor(model => model.RandomURL)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserURL)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Status)
                </th>*@
            @*<th></th>*@
        </tr>
        @{
            var isAlt = false;
            foreach (var item in Model)
            {

                var altClass = isAlt ? "alternating" : "";
                isAlt = !isAlt;
                <tr class="@altClass">
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    @*<td>
                        @item.WordList.Count()
                    </td>*@
                    @*<td>
                            @Html.DisplayFor(modelItem => item.RandomURL)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.UserURL)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Status)
                        </td>*@
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.ID })
                    </td>
                </tr>
            }
        }

    </table>
            }